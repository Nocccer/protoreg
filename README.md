# protoreg
A generator to create un/marshaler for registers slices ([]uint16,[]uint32) to structs. This is mostly needed for IoT protocols like Modbus.

# TODO

- [ ] Better byte support (e.g. uint16 register to byte without byte tag)
- [ ] Single bit support